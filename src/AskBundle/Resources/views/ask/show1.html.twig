{% extends 'base.html.twig' %}
{% block header %}
    <!-- Header -->
    <header class="masthead">
        <div class="container">

        </div>
    </header>

{% endblock %}

{% block other %}

    <section class="page-section" id="contact">
        <div class="container">
            <div class="row">
                <div class="col-lg-12 text-center">
                    <h2 class="section-heading text-uppercase"></h2>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-12">
                    <form id="contactForm" name="sentMessage" novalidate="novalidate">
                        <div class="row">
                            <div class="col-md-3">
                                <div class="form-group">
                                    <input class="form-control" id="name" type="text" value="{{ask.id}}" disabled >
                                    <p class="help-block text-danger"></p>
                                </div>
                                <div class="form-group">h
                                    <input class="form-control" id="email"  value="{% if ask.messageDate %}{{ ask.messageDate|date('Y-m-d') }}{% endif %}" disabled >
                                    <p class="help-block text-danger"></p>
                                </div>

                            </div>
                            <div class="col-md-6">
                                <div class="form-group">
                                    <textarea class="form-control" id="message" disabled>{{ ask.messageDetails }}</textarea>
                                    <p class="help-block text-danger"></p>
                                </div>
                            </div>
                            <div class="clearfix"></div>
                            <div class="col-lg-12 text-center">
                                <a class="btn btn-primary btn-l text-uppercase" href="{{ path('AskBundle_homeask_index1') }}">Back to the list</a>
                                <a class="btn btn-primary btn-l text-uppercase" href="{{ path('ask_deleteask', { 'id': ask.id }) }}">delete</a>





                                {# {{ form_start(delete_form) }}
                                     <input type="submit" class="btn btn-primary btn-l text-uppercase" value="Delete">
                                     {{ form_end(delete_form) }}#}


                            </div></div>
                    </form>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
